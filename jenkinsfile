#!/usr/bin/env groovy

pipeline {
  agent any

  stages {
    stage('install app1 to server') {
      steps {
        sh """
        cd app1/
        pwd
        chmod 400 LSD.pem
        ansible-playbook -i hosts main.yml
        """
      }

    }
  }
}

