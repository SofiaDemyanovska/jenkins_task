#!/usr/bin/env groovy

pipeline {
  agent any

  stages {
    stage('install app1 to host1') {
      steps {
        sh """
        cd app1/
        pwd
        chmod 400 LSD.pem
        ansible-playbook -i hosts main.yml
        """
      }
    }

    stage('install app2 to host2'){
      steps {
        sh """
        cd ..
        cd app2/
        pwd
        chmod LSD.pem
        ansible-playbook -i hosts main.yml
        """
      }
    }
  }
}

